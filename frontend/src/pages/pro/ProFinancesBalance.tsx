import FinancesBalance from '../FinancesBalance.tsx';

/** PRO Balance — reuses Hogar position panel (US5, T025). */
export default function ProFinancesBalance() {
  return <FinancesBalance namespace="pro" />;
}
