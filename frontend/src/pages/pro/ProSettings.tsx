import ProPlaceholderPage from './ProPlaceholderPage.tsx';

/** PRO settings shell (T009). Mirrors Hogar profile/book preferences. */
export default function ProSettings() {
  return (
    <ProPlaceholderPage
      activePath="/pro/settings"
      title="Ajustes PRO"
      description="Perfil y preferencias del libro. La configuración completa llega en la próxima etapa."
    />
  );
}
